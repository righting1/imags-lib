#全局配置
server:
  port: 8123
  servlet:
    context-path: /api
    # cookie 30 天过期
    session:
      cookie:
        max-age: 2592000
        domain: localhost
        same-site: None
        secure: true
spring:
  profiles:
    active: dev  # 默认使用开发环境
  application:
    name: imags-lib-backend
  # 开放更大的文件上传体积
  servlet:
    multipart:
      max-file-size: 10MB

  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/imagsLibDb
    username: root
    password: root
  redis:
    database: 0
    host: 172.16.10.10
    port: 6379
    password: root
    timeout: 5000
  # 空间图片分表
shardingsphere:
  datasource:
    names: yu_picture
    yu_picture:
      type: com.zaxxer.hikari.HikariDataSource
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/shizhan
      username: root
      password: root
  rules:
    sharding:
      tables:
        picture:
          actual-data-nodes: yu_picture.picture # 动态分表
          table-strategy:
            standard:
              sharding-column: spaceId
              sharding-algorithm-name: picture_sharding_algorithm  # 使用自定义分片算法
      sharding-algorithms:
        picture_sharding_algorithm:
          type: CLASS_BASED
          props:
            strategy: standard
            algorithmClassName: com.rightings.backed.manager.sharding.PictureShardingAlgorithm
  props:
    sql-show: true
mybatis-plus:
  configuration:
    # MyBatis 配置
    map-underscore-to-camel-case: false
    # 仅在开发环境打印日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    enable-sql-runner: true
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名
      logic-delete-value: 1 # 逻辑已删除值（默认为 1）
      logic-not-delete-value: 0 # 逻辑未删除值（默认为 0）
# 接口文档配置   http://127.0.0.1:8123/api/doc.html
knife4j:
  enable: true
  openapi:
    title: 接口文档
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.rightings.backed.controller
## 对象存储配置（需要从腾讯云获取）
#cos:
#  client:
#    host: xxx
#    secretId: xxx
#    secretKey: xxx
#    region: xxx
#    bucket: xxx

# 对象存储配置（需要从腾讯云获取）
cos:
  client:
    host: https://imags-1324735823.cos.ap-guangzhou.myqcloud.com
    secretId: AKIDQBl7LVUULyUpwoSs3zoPpFajT7YkkW0G
    secretKey: 3sVt2zjFWYMwHMCoFUUHqT0KadieNVSF
    region: ap-guangzhou
    bucket: imags-1324735823


#https://imags-1324735823.cos.ap-guangzhou.myqcloud.com/public/test/%40EventListener%E5%A4%84%E7%90%86.png

#{BucketName}.cos.{Region}.myqcloud.com


## 阿里云 AI 配置
aliYunAi:
  apiKey: sk-7a3acac23fd24d7eb382f7196096eeae








# Sa-Token配置
sa-token:
  cookie-samesite: None
  # token名称 (同时也是cookie名称)
  # token有效期，单位s 默认30天, -1代表永不过期
  timeout: 3600
  # token风格
  token-style: random-32
  # 是否尝试从 header 里读取 Token
  is-read-head: true
  # 是否开启自动续签
  auto-renew: true
  # 临时有效期，单位s，例如将其配置为 1800 (30分钟)，代表用户如果30分钟无操作，则此Token会立即过期
  activity-timeout: 1800
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时同端互斥)
  is-concurrent: true
  # 配置 Sa-Token 单独使用的 Redis 连接
  alone-redis:
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: 172.16.10.10
    # Redis服务器连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password: root
    # 连接超时时间
    timeout: 10s
  token-name: satoken